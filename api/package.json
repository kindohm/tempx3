{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "dotenv": "10.0.0",
    "express": "4.17.1",
    "hookshot": "0.1.0",
    "mysql2": "2.3.3",
    "pm2": "5.1.2",
    "sequelize": "^6.12.0-alpha.1",
    "sequelize-cli": "^6.3.0"
  },
  "devDependencies": {
    "nodemon": "2.0.15"
  },
  "scripts": {
    "start": "yarn run migrate && nodemon index.js",
    "deploy": "yarn && yarn run migrate:server && yarn pm2 start index.js",
    "deploy:update": "yarn && yarn run migrate:server && yarn pm2 restart index.js",
    "deploy:stop": "yarn pm2 stop index.js && yarn pm2 delete index.js",
    "hook:start": "yarn pm2 start githubHook.js",
    "hook:stop": "yarn pm2 stop githubHook.js && yarn pm2 delete githubHook.js",
    "migrate": "yarn sequelize-cli db:migrate",
    "migrate:server": "yarn run migrate --config ~/sequelize.json"
  }
}
